global:
    resolve_timeout: 5m

route:
    receiver: default
    group_by: ["alertname"]
    group_wait: 10s
    group_interval: 5m
    repeat_interval: 3h

receivers:
    - name: default
      webhook_configs:
          - url: "http://webhook:8080/alerts"
            send_resolved: true
